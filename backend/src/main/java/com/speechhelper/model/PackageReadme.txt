Model Package
-TODO conatins the model class which currently only does local data, but should interface with the database.
-The model receives commands, sends them to the command invoker, and has its data modified by the commands.