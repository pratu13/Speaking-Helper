Main Package
-Contains only Main.java, which contains only the method to start the springboot application.
-Annotations in the main tell spring to scan the com.speechhelper packages for relevant components and entities, and enables the use of JPA repositories
(see databasemanagerpackage for details on JPA repositories)