Database Manager Package
-Contains all the entity classes, which are spring annotated versions of our defined objects, that specify to the program how our custom objects should be stored,
and what instance variables each object has. 
-Contains all repository classes, which provide available methods for how the program will store and retrieve data from the database. Implements
JPA repositories which have specific methods to find objects by ID, find all objects, etc.